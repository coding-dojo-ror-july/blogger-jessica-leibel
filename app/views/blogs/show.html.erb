<h1><%= @user_blog.title %></h1>

<% @posts.each do |post| %>
    <a href='/blogs/<%= @user_blog.id %>/posts/<%= post.id %>/show'<h3><%= post.title %></h3></a>
    <p><%= truncate(post.content, length:10) %></p>
    
<% end %>    

<a href="/">View All Blogs</a>